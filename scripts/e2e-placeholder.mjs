// Placeholder e2e runner. The `feature` workflow replaces this with real
// browser tests (Playwright) that drive the agent UI end to end.
// Exits 0 so `npm run test:e2e` is wired and green on the initial scaffold.
console.log(
  "[e2e] No browser tests yet — implemented by the feature workflow. Skipping.",
);
process.exit(0);
